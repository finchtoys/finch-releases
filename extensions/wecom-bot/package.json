{
  "name": "finch-wecom-box",
  "version": "0.1.0",
  "description": "Connect WeCom intelligent bots to Finch through the official WebSocket SDK.",
  "author": "Finch Team",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=es2022 --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=dist/index.js",
    "test": "node scripts/smoke.mjs",
    "check": "npm run typecheck && npm run build && npm test",
    "prepublishOnly": "npm run check"
  },
  "dependencies": {
    "@wecom/aibot-node-sdk": "1.0.7"
  },
  "devDependencies": {
    "@finchtoys/minitool-api": "0.2.11",
    "@types/node": "^26.0.0",
    "esbuild": "0.28.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist/",
    "finch.json",
    "i18n/",
    "icons/",
    "README.md"
  ]
}
