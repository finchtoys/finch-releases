{
  "name": "finch-releases",
  "version": "0.0.0",
  "private": true,
  "description": "Community release site for Finch App — documentation, extensions, skills, packages and community configs.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "publish:all": "bash scripts/publish-all.sh",
    "publish:dry-run": "bash scripts/publish-all.sh --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/finchtoys/finch-releases.git"
  }
}
